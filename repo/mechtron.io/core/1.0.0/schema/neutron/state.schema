struct({fields:{

  mechtron: list( { of: 
     struct({fields:{
       seq_id : i64(),
       id: i64(),
       kind: string()
     }})
  }),
  mechtron_names: map( { value: 
    struct( {fields:{
      seq_id: i64(),
      id: i64()
    }})
  }),
  sim_id: struct( {fields:{
                              seq_id: i64(),
                              id: i64()
                            }}),
  mechtron_seq_id: i64(),
  mechtron_index: i64()

}})
