kind: neutron
name: Neutron
state: 
  artifact:
    path: schema/neutron/state.schema
     
message:
  inbound:
    ports:
    - name: ping
      artifact:
        path: schema/util/ping.schema
  outbound:
    ports:
    - name: pong
      artifact:
        path: schema/util/pong.schema

  extra:
    ports:
    - name: create_simtron
      artifact:
        path: schema/util/pong.schema

