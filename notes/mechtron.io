

WHAT IS A MECHTRON
A stateful WebAssembly object that can exist on the client or the server.  It's state can be replicated from server to client and vice versa.  Mechtrons can communicate to other mechtron's via message passing.



MECHTRON TEST

* a series of inputs and expected outputs
* written in RUST (or any language ~~~ compiles to WASM)
* given a STATE, test each port for expected result

* -> ensure no endless loop


SWITCH ON!!!!!!!


:


